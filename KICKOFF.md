# 开工文档：ncm-song-scorer 网易云音乐新歌自动评分系统

- **日期**：2026-08-15
- **状态**：v0.1.0 已落地（数据层 + 打分层 + CLI 全部实测可用）
- **维护人**：jiangliushi666

---

## 1. 项目定位

一句话：**每天自动盯网易云新歌榜，给每首新歌打一个 0-100 的「爆款潜力分」，并随时间积累数据训练机器学习模型，让分数越来越准。**

解决的问题：
- 每天有海量新歌上线，人工听歌筛选成本极高；
- 商业打分系统（Hit Song Science 类）闭源且收费；
- 本项目用全开源组件 + 网易云公开明文接口，个人可零成本自建。

## 2. 范围

**做：**
- 从网易云音乐新歌榜自动发现新歌（元数据 + 发行时间）；
- 每日快照记录每首歌的评论总数与平台热度值（pop），形成时间序列；
- 基于早期数据计算特征（讨论密度、评论增速、歌手资历、平台热度）；
- 启发式基线打分（零训练成本，冷启动即用）；
- ML 分类器训练（标签 = 发布 14 天内进入新歌榜），输出概率分。

**不做（明确出界）：**
- 不做歌单下载/整站爬取，只围绕榜单 + 被跟踪歌曲的指标；
- 不做实时流（当前按天粒度足够）；
- 不做前端界面（CLI 优先，Web 仪表盘放路线图）。

## 3. 架构

```
┌────────────────────────────────────────────────────────┐
│                    每日定时（scripts/daily.py）          │
└────────────────────────────────────────────────────────┘
   │
   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 数据层        │    │ 存储层        │    │ 特征层        │
│ api.py       │──▶ │ storage.py   │──▶ │ features.py  │
│ 明文API直连   │    │ SQLite       │    │ 7个核心特征   │
│ 新歌榜/详情/  │    │ songs        │    │ log1p压缩    │
│ 评论总数      │    │ snapshots    │    │ 防标签泄漏    │
└──────────────┘    │ charts/scores│    └──────┬───────┘
                    └──────────────┘           │
                          ▲                   ▼
                          │            ┌──────────────┐
                          └────────────│ 打分层        │
                                       │ scoring.py   │ 启发式 0-100（即用）
                                       │ model.py     │ ML 概率分（攒数据后）
                                       └──────┬───────┘
                                              ▼
                                       ┌──────────────┐
                                       │ CLI / 排名表  │
                                       │ cli.py       │
                                       └──────────────┘

可选扩展：audio_features.py（librosa 本地音频内容特征，与上面流程正交）
```

## 4. 数据流闭环

```
第 0 天   新歌入榜 ──入库──▶ 拍首日快照 ──▶ 启发式打分排名
第 1 天   再拍快照 ──▶ 出现"评论增速"特征 ──▶ 打分更准
...
第 N 天   榜单记录沉淀为标签（进过榜=1）
攒够样本 ──▶ train 训练 GBDT ──▶ 之后排名用 ML 概率分
```

核心设计决策（为什么这样做）：

| 决策 | 理由 |
|---|---|
| 明文 API 直连，不走 weapi 加密 | 实测 3 个端点免加密可用（见 docs/API_ENDPOINTS.md），零依赖零登录 |
| 以新歌榜为唯一发现源+标签源 | 热歌榜/飙升榜直连无数据；新歌榜 100 首/日天然就是"新歌+标签" |
| 启发式与 ML 并存 | 冷启动（前 2-3 周）启发式保证系统立刻有用；ML 成熟后接管 |
| 特征只用首日快照做训练 | 杜绝标签泄漏：预测时点只允许看到发布早期数据 |
| SQLite 单文件存储 | 单机自用足够，零运维，pandas 可直接读 |

## 5. 技术选型

- **Python ≥ 3.9**，核心依赖仅 `requests`；
- `scikit-learn`（GradientBoostingClassifier）——训练层可选依赖；
- `librosa`（可选）——本地音频内容特征，不装不影响主流程；
- MIT 开源协议。

## 6. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 端点探测、方案设计 | ✅ 2026-08-14 完成（3 个端点实测通过） |
| M1 | v0.1.0：数据层+存储+启发式打分+CLI | ✅ 已完成，端到端实测通过（拉榜 100 首→快照→打分） |
| M2 | **数据积累期**：每日定时跑 `daily`，攒 2-3 周 | ⏳ 从今天开始，挂上任务计划即可 |
| M3 | ML 训练：样本 ≥40/正样本 ≥10 后 `train`，看 AUC | ⏳ 依赖 M2 |
| M4 | 迭代：特征增强（歌手维度聚合）、音频特征融合、Web 仪表盘 | 📋 backlog |

## 7. 快速开始

```bash
git clone https://github.com/jiangliushi666/ncm-song-scorer.git
cd ncm-song-scorer
uv venv && uv pip install -r requirements.txt   # 或 pip

# 每天跑一次（挂 Windows 任务计划/cron）
python -m ncm_scorer daily

# 查看当前新歌排名
python -m ncm_scorer top -n 20

# 攒够数据后训练模型
python -m ncm_scorer train
```

## 8. 风险与合规

- **接口稳定性**：明文接口非官方契约，可能变动。已封装在 `api.py` 单文件内，变更只改一处；
- **限速**：客户端默认 1 req/s，快照上限 200 首/日，属个人研究量级；请勿调低限速或放大上限去压测；
- **用途边界**：仅个人研究/学习。数据归网易云音乐所有，勿二次分发原始数据；请遵守平台服务条款；
- **模型局限**：学术界共识是纯早期数据预测爆款天花板有限（音频特征约解释 69% 方差），分数应作排序参考而非绝对判断。

## 9. 目录导览

```
ncm_scorer/
├── api.py            # 直连客户端（3个实测端点）
├── storage.py        # SQLite 四张表
├── features.py       # 7 个特征（FEATURE_NAMES）
├── scoring.py        # 启发式打分 heuristic-v1
├── model.py          # ML 训练/推理（gbc-v1）
├── pipeline.py       # 业务编排（daily 流程）
├── audio_features.py # 可选 librosa 特征
└── cli.py            # 6 个子命令
scripts/daily.py      # 定时任务入口
tests/test_offline.py # 离线单测（5 个，不联网）
docs/API_ENDPOINTS.md # 端点实测记录
```

## 10. 下一步行动清单

- [ ] 把 `scripts/daily.py` 挂上 Windows 任务计划（每天 09:00）
- [ ] 连续跑 14 天后执行 `python -m ncm_scorer train`，记录首版 AUC
- [ ] 回填：榜单历史回溯（用 first_seen 之前的数据扩充早期样本）
- [ ] 评估：加入「歌手近 90 天上榜率」特征（从 charts 表聚合）
