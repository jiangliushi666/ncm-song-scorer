# 开工文档：ncm-song-scorer 网易云音乐新歌自动评分系统

- **日期**：2026-08-15
- **状态**：v0.2.0（启发式 v2 + 可解释排名页 + ML 标签修正 + 邻域负样本）
- **维护人**：jiangliushi666

---

## 1. 项目定位

一句话：**每天自动盯网易云新歌榜，给每首新歌打一个 0-100 的「爆款潜力分」，并随时间积累数据训练机器学习模型，让分数越来越准。**

解决的问题：
- 每天有海量新歌上线，人工听歌筛选成本极高；
- 商业打分系统（Hit Song Science 类）闭源且收费；
- 本项目用全开源组件 + 网易云公开明文接口，个人可零成本自建。

## 2. 范围

**做：**
- 从网易云音乐新歌榜自动发现新歌（元数据 + 发行时间）；
- 从榜上歌手的热门近作补「未上榜」负样本（邻域发现，每日有上限）；
- 每日快照记录每首歌的评论总数与平台热度值（pop），形成时间序列；
- 基于早期数据计算特征（讨论密度、评论增速、歌手资历、歌手上榜热、是否 Live）；
- 启发式基线打分（零训练成本，冷启动即用，Live/翻唱降权）；
- ML 分类器训练（标签 = 新歌榜最佳名次 ≤ 20），输出概率分；
- GitHub Pages 静态排名页（分项明细 + Live/近 7 天筛选 + 页内试听）。

**不做（明确出界）：**
- 不做歌单下载/整站爬取，只围绕榜单 + 被跟踪歌曲的指标；
- 不做实时流（当前按天粒度足够）。

## 3. 架构

```
┌────────────────────────────────────────────────────────┐
│          每日定时（GitHub Actions 北京时间 09:30）        │
│                    scripts/daily.py                    │
└────────────────────────────────────────────────────────┘
   │
   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 数据层        │    │ 存储层        │    │ 特征层        │
│ api.py       │──▶ │ storage.py   │──▶ │ features.py  │
│ 明文API直连   │    │ SQLite       │    │ 9 个核心特征  │
│ 新歌榜/详情/  │    │ songs        │    │ log1p压缩    │
│ 评论/歌手热门 │    │ snapshots    │    │ 防标签泄漏    │
└──────────────┘    │ charts/scores│    └──────┬───────┘
                    └──────────────┘           │
                          ▲                   ▼
                          │            ┌──────────────┐
                          └────────────│ 打分层        │
                                       │ scoring.py   │ heuristic-v2
                                       │ model.py     │ gbc-v1（攒数据后）
                                       └──────┬───────┘
                                              ▼
                                       ┌──────────────┐
                                       │ CLI + Pages  │
                                       │ cli.py       │
                                       │ build_site.py│
                                       └──────────────┘
```

## 4. 数据流闭环

```
第 0 天   新歌入榜 ──入库──▶ 邻域近作（未上榜）──▶ 拍首日快照 ──▶ heuristic-v2 排名
第 1 天   再拍快照 ──▶ 出现"评论增速"特征 ──▶ 打分更准
...
攒够样本 ──▶ train（标签 = 最佳名次 ≤ 20）──▶ 之后排名可用 ML 概率分
```

核心设计决策（为什么这样做）：

| 决策 | 理由 |
|---|---|
| 明文 API 直连，不走 weapi 加密 | 实测 4 个端点免加密可用（见 docs/API_ENDPOINTS.md），零依赖零登录 |
| 新歌榜做发现源，邻域近做负样本 | 「进过新歌榜」对从榜单发现的歌恒为 1；未上榜近作才能拉开正负 |
| 标签改为最佳名次 ≤ 20 | 在已上榜歌曲内部也能区分冲榜强弱，冷启动即可训练 |
| 启发式与 ML 并存 | 冷启动 heuristic-v2 立刻有用；ML 成熟后接管 |
| 特征只用首日快照做训练 | 杜绝标签泄漏：预测时点只允许看到发布早期数据 |
| 歌手上榜热忽略 24h 内记录 | 同日专辑多首不会互相抬分 |
| SQLite 单文件 + 私有数据仓 | 单机自用足够；Actions 把 db 持久化到 ncm-scorer-data |

## 5. 技术选型

- **Python ≥ 3.9**，核心依赖仅 `requests`；
- `scikit-learn`（GradientBoostingClassifier）——训练层可选依赖；
- `librosa`（可选）——本地音频内容特征，不装不影响主流程；
- MIT 开源协议。

## 6. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 端点探测、方案设计 | ✅ 2026-08-14 完成（明文端点实测通过） |
| M1 | v0.1.0：数据层+存储+启发式打分+CLI | ✅ 已完成 |
| M1b | GitHub Actions 日更 + Pages 排名页 + 试听 | ✅ 2026-08-15 |
| M1c | 歌手 90 天上榜热特征 + 训练 workflow | ✅ 2026-08-15 |
| M2 | v0.2.0：heuristic-v2 / 可解释榜 / 标签修正 / 邻域负样本 | ✅ 2026-08-15 |
| M3 | 数据积累期：每日 Actions 跑 `daily`，攒 2-3 周 | ⏳ 进行中 |
| M4 | 首版 `train`，记录 AUC；评估是否切到 ML 排名 | ⏳ 依赖 M3 |
| M5 | 回填榜史、音频特征融合 | 📋 backlog |

## 7. 快速开始

```bash
git clone https://github.com/jiangliushi666/ncm-song-scorer.git
cd ncm-song-scorer
uv venv && uv pip install -r requirements.txt   # 或 pip

# 每天跑一次（线上已由 GitHub Actions 在北京时间 09:30 执行）
python -m ncm_scorer daily

# 查看当前新歌排名
python -m ncm_scorer top -n 20

# 生成本地排名页
python scripts/build_site.py --db ncm_scorer.db --out site/index.html

# 攒够数据后训练模型
python -m ncm_scorer train
```

线上榜：https://jiangliushi666.github.io/ncm-song-scorer/

## 8. 风险与合规

- **接口稳定性**：明文接口非官方契约，可能变动。已封装在 `api.py` 单文件内，变更只改一处；
- **限速**：客户端默认 1 req/s，快照上限 200 首/日，邻域发现 ≤12 歌手/20 首，属个人研究量级；请勿调低限速或放大上限去压测；
- **用途边界**：仅个人研究/学习。数据归网易云音乐所有，勿二次分发原始数据；请遵守平台服务条款；
- **模型局限**：学术界共识是纯早期数据预测爆款天花板有限（音频特征约解释 69% 方差），分数应作排序参考而非绝对判断。
- **标签局限**：最佳名次与 pop/评论相关，首版 AUC 可能偏乐观；邻域负样本积累后会更接近「能否上榜」问题。

## 9. 目录导览

```
ncm_scorer/
├── api.py            # 直连客户端（4 个实测端点）
├── storage.py        # SQLite 四张表
├── features.py       # 9 个特征（FEATURE_NAMES）+ title_flags
├── scoring.py        # 启发式打分 heuristic-v2
├── model.py          # ML 训练/推理（gbc-v1，标签=最佳名次≤20）
├── pipeline.py       # 业务编排（daily + 邻域发现）
├── audio_features.py # 可选 librosa 特征
└── cli.py            # 6 个子命令
scripts/daily.py      # 定时任务入口
scripts/build_site.py # GitHub Pages 静态页
tests/test_offline.py # 离线单测（不联网）
docs/API_ENDPOINTS.md # 端点实测记录
```

## 10. 下一步行动清单

- [x] GitHub Actions 每日采集（替代本机任务计划）
- [x] 加入「歌手近 90 天上榜天数」特征（排除自身、忽略同日）
- [x] 修正 ML 标签（不再用「进过榜」这种恒正标签）
- [x] 歌手邻域负样本发现
- [ ] 连续跑 14 天后执行 `python -m ncm_scorer train`，记录首版 AUC
- [ ] 回填：榜单历史回溯（用 first_seen 之前的数据扩充早期样本）
- [ ] 评估：邻域负样本够了之后，是否把标签切回「是否上榜」
